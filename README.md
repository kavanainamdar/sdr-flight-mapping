# sdr-flight-mapping
This is a project that uses signals from an sdr antenna, transmits that data for flight signals into a JSON and API, then is used by the front end to showcase the flight markers on a global map in React. 
